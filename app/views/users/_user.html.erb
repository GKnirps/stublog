<% c = "user" %>
<% c = "author" if user.author? %>
<% c = "admin" if user.admin? %>
<li class="<%= c %>">
	<%= link_to user.name, user %>, <%= user.email %>
	<% if current_user.admin? or current_user?(user) then %>
		<%= link_to "delete", confirm_destroy_user_path(user) %>
	<% end %>
</li>
