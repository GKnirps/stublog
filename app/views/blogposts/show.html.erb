<% provide :title, @blogpost.caption %>
<%= render @blogpost %>

<% if Rails.configuration.comments_active then %>
  <% if @comments.any? then %>
	  <div class="comments">
		  <ul>
        <% @comments.each do |comment| %>
		      <%= render 'comments/comment', comment: comment, blogpost: @blogpost %>
        <% end %>
		  </ul>
	  </div>
  <% end %>
  <%= render 'comments/new_comment_form', comment: @comment, blogpost: @blogpost, author: @author, predecessor: @predecessor %>
<% end %>
